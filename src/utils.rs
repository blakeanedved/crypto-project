//use rug::{Integer, integer::Order};
//use crate::aes;

pub fn test() {
    //let x = Integer::from_digits(&[1u8, 2, 3, 4], Order::Msf);
    //let x = Integer::from(69i32);
    //let rsa_key = crate::rsa::rsa_key_gen();
    //let enc = crate::rsa::rsa_encrypt(&rsa_key.0, x);
    ////let dec = crate::rsa::rsa_decrypt(&rsa_key.1, &enc.to_digits::<u8>(Order::Msf));
    //let dec = crate::rsa::rsa_decrypt(&rsa_key.1, enc);
    //println!("dec={:?}", dec);

    //println!("{}", Integer::from_digits::<u8>(&Integer::from(69i32).to_digits::<u8>(Order::Msf)[..], Order::Msf));

    //let mut rng = rug::rand::RandState::new();
    //rng.seed(&Integer::from(std::time::SystemTime::now().duration_since(std::time::UNIX_EPOCH).unwrap().as_nanos()));
    //println!("{}", crate::miller_rabin::miller_rabin(16, 7, &mut rng));
    //println!("{}", crate::miller_rabin::miller_rabin(16, 7, &mut rng));
}
